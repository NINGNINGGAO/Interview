#include <stdio.h>

#define MAX_NUM 100

/*
* Welcome to vivo !
*/

int solution(int n, int boxes[]) {

    // TODO Write your code here

    return 0;
}

int main()
{
    int A[MAX_NUM] = {0};
    int len = 0;
    do {
        scanf("%d", &A[len++]);
    } while (getchar()!='\n');

    printf("%d",solution(len, A));

    return 0;
}